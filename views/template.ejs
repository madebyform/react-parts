<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>React.parts – A catalog of React components</title>
  <meta name="description" content="An unofficial catalog of React components for both Web and Native.">

  <link href="//cdnjs.cloudflare.com/ajax/libs/normalize/3.0.2/normalize.min.css" rel="stylesheet">
  <link href="//fonts.googleapis.com/css?family=Source+Sans+Pro:200,400,600|Source+Code+Pro:400" rel="stylesheet">

  <link rel="prefetch" href="/api/components/web">
  <link rel="prefetch" href="/api/components/native">

  <style>
    /* Disable overscroll (bouncing scroll effect on OS X) */
    html, body {
      width: 100%;
      height: 100%;
      overflow: hidden;
      background: #ebedf2;
      margin: 0;
    }
    /* This class keeps the content scrollable and allows to programmatically change
     * the scroll position (which becomes broken with the css for disabling overscroll) */
    #container, .u-scrollable {
      width: 100%;
      height: 100%;
      min-height: 100%;
      overflow-x: hidden;
      overflow-y: auto;
      -webkit-overflow-scrolling: touch;
    }
    /* Temporary hack for Safari */
    .u-displayFlex {
      display: -webkit-box;
      -webkit-align-items: center;
    }

    /* Make visited links slightly brighter */
    .u-visited {
      color: #fff;
    }
    .u-visited:visited {
      color: #777;
    }

    /* For search highlights */
    mark {
      background: transparent;
      color: inherit;
      border-bottom: 1px solid #ebedf2;
    }

    ::-webkit-input-placeholder {
      color: #fff;
      font-weight: 300;
    }
    ::-moz-placeholder {
      color: #fff;
      font-weight: 300;
    }
    :-ms-input-placeholder {
      color: #fff;
      font-weight: 300;
    }

    @media only screen and (max-width: 768px) {
      html {
        font-size: 80%;
      }
      .u-forceSmall16 {
        /* Prevent zoom on input focus */
        font-size: 16px !important;
      }
      .u-hideSmall {
        display: none !important;
      }
    }
    @media only screen and (max-width: 1023px) {
      .u-hideMedium {
        display: none !important;
      }
    }
  </style>
</head>
<body>
  <div id="container"><%- output %></div>

  <script>
    window.initialData = <%- initialData %>;
  </script>
  <%- cachify_js("/app.min.js") %>
</body>
</html>
