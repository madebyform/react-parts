{
  "private": true,
  "dependencies": {
    "async": "^0.9.0",
    "babel": "^4.7.16",
    "babelify": "^5.0.4",
    "browserify": "^9.0.3",
    "ejs": "^2.3.1",
    "react": "^0.13.1",
    "request": "^2.54.0"
  },
  "devDependencies": {
    "uglifyjs": "^2.4.10"
  },
  "scripts": {
    "build": "npm run build:client && npm run build:index",
    "build:index": "node build.js",
    "build:client": "browserify app.jsx -t babelify --outfile app.js && uglifyjs app.js -o app.min.js"
  }
}
